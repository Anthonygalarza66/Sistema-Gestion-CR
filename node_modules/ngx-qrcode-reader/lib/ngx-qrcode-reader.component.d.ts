import { EventEmitter, OnChanges, OnInit, Renderer2, ElementRef } from '@angular/core';
export declare class NgxQrcodeReaderComponent implements OnChanges, OnInit {
    private renderer;
    cssClass: string;
    showQRCode: boolean;
    fileValue: string;
    qrCodeElement: ElementRef;
    qrContentResult: EventEmitter<any>;
    qrReader: any;
    constructor(renderer: Renderer2);
    ngOnInit(): void;
    ngOnChanges(): void;
    decode(value: any): void;
    render(): void;
    renderElement(element: any): void;
}
